name: COMBO-1 + DISP Research

on:
  push:
    branches: [ main ]
    paths:
      - 'research/crypto/**'
      - '.github/workflows/combo1-backtest.yml'
  workflow_dispatch:

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas scipy scikit-learn requests
      - name: Run COMBO-1 walk-forward backtest
        env:
          COMBO_OUT: combo1_results
          COMBO_CACHE: .combo1_cache
        run: python research/crypto/combo1_backtest_v2.py
      - name: Run DISP-1 event study
        env:
          COMBO_DAILY: combo1_results/daily_results.csv
          DISP_OUT: disp1_results
          DISP_CACHE: .disp1_cache
        run: python research/crypto/disp1_event_study.py
      - name: Run DISP-2 extensions
        env:
          DISP1_DAILY: disp1_results/event_days.csv
          DISP2_OUT: disp2_results
        run: python research/crypto/disp2_extensions.py
      - name: Run DISP-3 reversal variants
        env:
          COMBO_DAILY: combo1_results/daily_results.csv
          DISP3_OUT: disp3_results
          DISP_CACHE: .disp1_cache
        run: python research/crypto/disp3_reversal_variants.py
      - name: Upload COMBO-1 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: combo1-results-${{ github.run_id }}
          path: combo1_results/
          if-no-files-found: warn
          retention-days: 14
      - name: Upload DISP-1 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: disp1-results-${{ github.run_id }}
          path: disp1_results/
          if-no-files-found: warn
          retention-days: 14
      - name: Upload DISP-2 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: disp2-results-${{ github.run_id }}
          path: disp2_results/
          if-no-files-found: warn
          retention-days: 14
      - name: Upload DISP-3 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: disp3-results-${{ github.run_id }}
          path: disp3_results/
          if-no-files-found: warn
          retention-days: 14
